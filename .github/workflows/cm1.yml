name: Comanda manuală cu date de intrare
on: 
  workflow_dispatch:
    inputs:
      opțiunileMele:
        description: 'Ce alegeți?'
        required:    true
        type:        choice
        options:
                     - 'unu'
                     - 'doi'
                     - 'trei'
jobs: 
  mesagerie: 
    runs-on: ubuntu-latest
    steps:
      - run: >
               echo Salut!
               Am optat pentru
               ${{inputs.opțiunileMele}}.
